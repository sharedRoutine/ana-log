---
description: TypeScript coding conventions
globs: ['**/*.ts', '**/*.tsx']
---

# TypeScript

- Do not define explicit types when TypeScript can infer them
- Avoid using tacit (point-free) function calls; prefer explicit arrow functions with parameters
